{{#if logueado}}
    <nav>
        {{#if logueado}}
            <span><strong>Hola 🏋️{{nombre}}</strong></span>

            {{#if admin}}
                <a href="/admin">✅Panel Admin</a>
            {{else}}
                <span>⛔Panel admin</span>
            {{/if}}
            <a href="/">🏠Home</a>
            <a href="/productos">🛒Productos</a>
            <a href="/logout">👋Cerrar sesion</a>
        {{else}}
            <a href="/login">👋Ingresar</a>
        {{/if}}
        
    </nav>
{{/if}}