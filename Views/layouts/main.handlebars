<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
</head>

<style>
    *{
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }
    a {
        text-decoration: none;
        color: inherit;
    }
    main {
        padding: 20px;
        min-height: calc(100vh - 160px);
    }
    nav{
        margin-bottom: 20px;
        display: flex;
        justify-content: space-around;
        background-color: #ff9a9aa0;
        padding: 10px;
    }
    h2 {
        color: #333;
        font-family: Arial, sans-serif;
        text-align: center;
        margin-top: 5px;
    }
    .loginForm {
        max-width: 300px;
        margin: auto;
        display: flex;
        flex-direction: column;
        gap: 10px;
        justify-content: center;
        align-items: center;
        padding: 20px;
        border: 1px solid #030303;
        border-radius: 5px;
    }
    .ManejoProductos {
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        align-items: center;
        min-height: 90%;
        width: 100%;
        margin-top: 15px;
    }
    .botoneraManejoProductos {
        max-width: 30%;
        display: flex;
        flex-direction: column;
        gap: 10px;
        margin-right: 20px;
    }
    .visualManejoProductos {
        max-width: 70%;
        display: flex;
        flex-direction: row;
        align-items: flex-start;
    }
    li {
        list-style-type: none;
        margin: 15px;
    }
    li img {
        width: 20px;;
        height: 20px;
        margin-right: 10px;
        margin-bottom: 5px;
    }
    .contenedorForm {
        height: 300px;
        width: 300px;
        border: 1px solid #000;
        margin-bottom: 20px;
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        z-index: 10;
        background: rgba(0,0,0,0.5);
        display: none;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        
    }
    footer {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 60px;
        background-color: #f1f1f1;
    }
</style>

<body>
    {{> navbar}}

    <main>
        {{#if logueado}}
            {{{body}}}
        {{else}}
            <h2>Por favor, inicie sesión para acceder a esta página.</h2>
            {{> login}}
        {{/if}}
    </main>

    {{> footer}}
</body>

</html>